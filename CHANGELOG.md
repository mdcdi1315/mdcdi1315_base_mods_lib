### Now releasing 1.0.30:

-> [NeoForge]: Fixed incorrect event dispatcher selection when adding a new server-side resource reload listener.

-> Config API: Static fields in configuration classes are no longer considered at all, and when instancing a new
ReflectedConfigFieldData instance causes an exception to be thrown.